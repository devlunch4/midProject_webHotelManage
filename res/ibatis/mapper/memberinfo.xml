<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE sqlMap
 	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
 	"http://ibatis.apache.org/dtd/dtd-map-2.dtd">

<sqlMap namespace="memberinfo">

	<select id="getMemberInfo" parameterClass="memberinfoVO"
		resultClass="memberinfoVO">
		select mem_id, mem_pass, mem_name
		from memberinfo
		where
		mem_id = #mem_id#
		and mem_pass = #mem_pass#
	</select>

	<select id="selectById" parameterClass="string"
		resultClass="string">
		SELECT mem_id FROM memberinfo WHERE mem_id = #data#
	</select>


	<insert id="insertMember" parameterClass="memberinfoVO">
		INSERT INTO memberinfo ( 
		mem_id,
		mem_pass,
		mem_name,
		mem_bir,
		mem_tel,
		mem_addr,
		mem_email,
		power
		)
		VALUES(
		#mem_id#,
		#mem_pass#,
		#mem_name#,
		TO_DATE(#mem_bir#, 'YYYY-MM-DD'),
		#mem_tel#,
		#mem_addr#,
		#mem_email#,
		#power#
		)
	</insert>

</sqlMap>




 	
 	